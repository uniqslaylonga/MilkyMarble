DIFFERENT TYPES OF ERROR:
1. SYNTAX ERROR
2. LOGICAL ERROR (nagrurun 'yung program pero may mali.)


ASSIGMNETN 

the differenct symbols and functions of a flow chart

/*
    double first;
    char op;
    double second;

    printf("\nEnter your first number: \n");
    scanf("%lf", &first);
    printf("\nEnter your operator: ");
    scanf(" %c", &op);
    printf("\nEnter your second number: ");
    scanf("%lf", &second);

    if(op == '+'){
        printf("%f", first+second);
    } else if (op == '-'){
    printf("%f", first-second);}
    else if (op == '*'){
        printf("%f", first*second);
    }else if (op == '/'){
    printf("%f", first/second);}
    else {printf("invalid operator");}


    return 0;
    /*

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int luckynum = 21;
    int guess;
    int guesscount = 0;
    int guesslimit = 10;
    int outofguesses = 0;

    while (guess != luckynum && outofguesses == 0){
        if (guesscount < guesslimit){
    printf("Enter your guess: ");
    scanf("%d", &guess);
    guesscount++; }
    else {outofguesses=1;}
    }
    if (outofguesses==1){
        printf("You're out of guesses!");
    }
    else {printf("You win");
    }
    return 0;
}


CODE FOR LOOPS
int luckynum = 21;
    int guess;
    int guesscount = 0;
    int guesslimit = 10;
    int outofguesses = 0;

    while (guess != luckynum && outofguesses == 0){
        if (guesscount < guesslimit){
    printf("Enter your guess: ");
    scanf("%d", &guess);
    guesscount++; }
    else {outofguesses=1;}
    }
    if (outofguesses==1){
        printf("You're out of guesses!");
    }
    else {printf("You win");
    }

#include <stdio.h>
#include <stdlib.h>
#define pf printf
#define sf scanf
int main()

{
   int age;

    pf("\nENTER AGE: ");
    sf("%d", &age);

    if ((age >= 18) && (age <=100))
    {
        pf("\nQUALIFY TO VOTE!");
    }
    else if ((age >= 1) && (age <= 17))
    {
       pf("\nUNDERAGE! ");
    }
    else
    {
        pf("\nINVALID INPUT");
    }
      return 0;
}






